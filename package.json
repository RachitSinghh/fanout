{
  "name": "fanout",
  "version": "0.1.0",
  "private": true,
  "description": "Fanout — Gmail-native personalized bulk sender (Chrome extension, MV3)",
  "scripts": {
    "build": "pnpm --filter @fanout/extension build",
    "dev": "pnpm --filter @fanout/extension dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.9.0"
}
